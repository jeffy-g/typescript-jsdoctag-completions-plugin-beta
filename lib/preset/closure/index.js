/*! For license information please see index.js.LICENSE.txt */
(()=>{"use strict";var n={3(n,e,t){const o=t(16),r=t(6),i=t(12),s=["abstract","const","constructor","define","deprecated","dict","enum","export","extends","externs","fileoverview","final","implements","implicitCast","inheritDoc","interface:record","lends","license:preserve","noalias","nocollapse","nocompile","noinline","nosideeffects:modifies","override","package","param","polymer","polymerBehavior","private","protected","public","return","struct","suppress","template","this","throws","type","typedef","unrestricted"],a=[],c=(l={const:"const-const-type",enum:"enum-type",throws:"throws-type",extends:"extends-type",implements:"implements-type",interface:"interface-record",record:"interface-record",lends:"lends-objectname",param:"param-type-varname-description",return:"return-type-description",template:"template-t",this:"this-type",fileoverview:"fileoverview-description",license:"license-preserve-description",preserve:"license-preserve-description",nosideeffects:"nosideeffects-modifies-thisarguments",modifies:"nosideeffects-modifies-thisarguments",type:"type-type",typedef:"typedef-type",define:"define-type-description",export:"export-export-sometype",deprecated:"deprecated-description",suppress:"suppress-warninggroup1warninggroup2"},Object.assign(Object.create(null),l));var l;const u="abstract|noinline|constructor|dict|implicitCast|nosideeffects|interface|inheritDoc|externs|override|final|struct|package|private|protected|public|record|polymerBehavior|unrestricted|noalias|nocompile|nocollapse".split("|"),p="deprecated|license|preserve|fileoverview".split("|"),d="param|return|type|typedef|this|extends|implements|template|throws|export|const|define|enum|suppress|lends|modifies".split("|"),f={marker:u,simple:p,complexity:d,provideTagData:n=>n?s:a,getExtraInfo(n){const e="https://github.com/google/closure-compiler/wiki";let t;return t="abstract"===n?`${e}/@abstract-classes-and-methods`:`${e}/Annotating-JavaScript-for-the-Closure-Compiler#${c[n]||n}`,`More info - Closure Compiler wiki [${n}](${t})`},getTagTypeInfo:(0,i.tagTypeInfoTemplate)(u,d,[])};n.exports=(0,r.compose)(f,o.i18n,{block:{const:"@const [{<type>}]",define:"@define {<type>} <description>",deprecated:"@deprecated <description>",fileoverview:"@fileoverview <description>",enum:"@enum {<type>}",extends:"@extends {<type>}",export:"@export [{SomeType}]",implements:"@implements {<type>}",lends:"@lends {objectName}",license:"@license [<description>]",modifies:"@modifies {<this|arguments>}",return:"@return {<type>} <description>",param:"@param [{<type>}] <name> <description>",this:"@this {<type>}",type:"@type {<type>}",typedef:"@typedef {<type>}",throws:"@throws {<type>}",suppress:"@suppress {<warningGroup1,warningGroup2>}"},inline:{}})},6(n){n.exports=require("../../di")},12(n,e){Object.defineProperty(e,"__esModule",{value:!0}),e.tagTypeInfoTemplate=function(n,e,t){return o=>n.includes(o)?"*markerTag (no args)*":t.includes(o)?"*inlineTag*":e.includes(o)?"*complexityTag (multi-part syntax)*":"*simpleTag description or some text*"}},16(n,e,t){Object.defineProperty(e,"__esModule",{value:!0}),e.i18n=void 0;const o=t(28),r=o.closurePresetDocsTranslations.en.blockDocs,i=o.closurePresetDocsTranslations.en.inlineDocs;e.i18n={en:{blockDocs:r,inlineDocs:i},cs:{blockDocs:{abstract:"Označí třídu nebo metodu jako abstraktní pro potřeby kontroly typů.\n",const:"Označí proměnnou/vlastnost jako pouze pro čtení (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Označí funkci jako konstruktor používaný s `new`.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Deklaruje konstantu při kompilaci, kterou lze přepsat pomocí `--define`.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Označí symbol jako zastaralý; použití vyvolá varování kompilátoru.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Deklaruje slovníkový objekt s dynamickými vlastnostmi (přístup přes hranaté závorky).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Deklaruje enum typ s konečnou množinou hodnot.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Exportuje/zachová název symbolu (např. s `--generate_exports`) a brání přejmenování.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Deklaruje, že třída/rozhraní dědí z jiného typu.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Deklaruje soubor jako externs soubor.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"JSDoc blok na úrovni souboru (informace o souboru a suppressions).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Zabrání rozšíření třídy nebo přepsání metody.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Deklaruje, že konstruktor implementuje rozhraní.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Pouze externs: dovolí přiřazení libovolného typu, ale čtení vrací deklarovaný typ.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Převezme dokumentaci z přepisovaného členu (implikuje `@override`).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Označí funkci jako rozhraní definující požadované členy.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Označí funkci jako rozhraní definující požadované členy.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Považuje klíče objektového literálu za vlastnosti jiného objektu.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Zachová licenční/copyright komentář ve výstupu.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Zachová licenční/copyright komentář ve výstupu.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Pouze externs: deklaruje, že volání nemá vedlejší efekty; podporuje `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Zabrání sloučení (collapse) vlastnosti do proměnné.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Soubor se parsuje, ale nekompiluje.\n\n```js\n/** @nocompile */\n```\n",noinline:"Experimentální: zakáže inlining optimalizace.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Pouze externs: deklaruje, že volání nemá vedlejší efekty; podporuje `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Pouze externs: deklaruje, že volání nemá vedlejší efekty; podporuje `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Označí, že člen přepisuje člen nadřazené třídy/rozhraní.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private: přístupné jen z téhož adresáře.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Dokumentuje typy parametrů funkce/metody.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Označí třídu jako Polymer pro Polymer Pass.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Označí globální objekt jako Polymer behavior.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Zachová licenční/copyright komentář ve výstupu.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Private: přístupné jen v rámci stejného souboru.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Protected: přístupné v podtřídách (+ ve stejném souboru).\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Public: veřejný člen (výchozí; zřídka se píše explicitně).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Označí funkci jako rozhraní definující požadované členy.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Dokumentuje návratový typ funkce/metody.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Deklaruje objekt s pevnou strukturou (přístup tečkou, bez nových vlastností).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Potlačí vybrané varování kompilátoru.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Deklaruje generické parametry typů.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Specifikuje typ `this` uvnitř funkce.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Dokumentuje vyhazované výjimky (v externs se používá pro analýzu vedlejších efektů).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Deklaruje typ proměnné/vlastnosti/výrazu.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Vytvoří alias typu pro komplexní typy.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Deklaruje třídu jako ani `@struct`, ani `@dict` (povolí dynamické vlastnosti).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},de:{blockDocs:{abstract:"Kennzeichnet eine Klasse oder Methode als abstrakt für die Typprüfung.\n",const:"Markiert eine Variable/Property als schreibgeschützt (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Markiert eine Funktion als Konstruktor zur Verwendung mit `new`.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Deklariert eine Compile-Time-Konstante, die per `--define` überschrieben werden kann.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Markiert ein Symbol als veraltet; die Verwendung erzeugt eine Warnung.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Deklariert ein Dictionary-Objekt mit dynamischen Properties (Klammernotation).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Deklariert einen Enum-Typ mit endlich vielen Werten.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Exportiert/behält einen Symbolnamen (z. B. mit `--generate_exports`) und verhindert Umbenennung.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Deklariert, dass eine Klasse/ein Interface einen anderen Typ erweitert.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Deklariert die Datei als Externs-Datei.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"JSDoc-Block auf Dateiebene (Dateiinformationen und Suppressions).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Verhindert das Erweitern einer Klasse oder das Überschreiben einer Methode.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Deklariert, dass ein Konstruktor ein Interface implementiert.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Nur Externs: erlaubt Zuweisungen beliebiger Typen, aber Lesen als deklarierter Typ.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Übernimmt die Dokumentation des überschriebenen Members (impliziert `@override`).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Markiert eine Funktion als Interface, das erforderliche Members beschreibt.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Markiert eine Funktion als Interface, das erforderliche Members beschreibt.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Behandelt Keys eines Objekt-Literals als Properties eines anderen Objekts.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Erhält einen Lizenz-/Copyright-Kommentar im Output.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Erhält einen Lizenz-/Copyright-Kommentar im Output.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Nur Externs: deklariert, dass ein Aufruf keine Side Effects hat; unterstützt `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Verhindert das Zusammenfalten (collapse) einer Property in eine Variable.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Parst die Datei, kompiliert sie aber nicht.\n\n```js\n/** @nocompile */\n```\n",noinline:"Experimentell: verhindert Inlining-Optimierungen.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Nur Externs: deklariert, dass ein Aufruf keine Side Effects hat; unterstützt `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Nur Externs: deklariert, dass ein Aufruf keine Side Effects hat; unterstützt `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Markiert, dass ein Member ein Member aus Superklasse/Interface überschreibt.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private: nur aus demselben Verzeichnis zugreifbar.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Dokumentiert Parametertypen für Funktionen/Methoden.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Kennzeichnet eine Klasse als Polymer für den Polymer Pass.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Kennzeichnet ein globales Objekt als Polymer-Behavior.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Erhält einen Lizenz-/Copyright-Kommentar im Output.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Private: nur im selben File zugreifbar.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Protected: in Subklassen (+ im selben File) zugreifbar.\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Public: öffentliches Member (Standard; selten explizit).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Markiert eine Funktion als Interface, das erforderliche Members beschreibt.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Dokumentiert den Rückgabetyp einer Funktion/Methode.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Deklariert ein Objekt mit fester Struktur (Punktzugriff, keine neuen Properties).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Unterdrückt ausgewählte Compiler-Warnungen.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Deklariert generische Typparameter.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Gibt den Typ von `this` innerhalb einer Funktion an.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Dokumentiert geworfene Exceptions (in Externs für Side-Effect-Analyse).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Deklariert den Typ einer Variable/Property/eines Ausdrucks.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Erstellt einen Typ-Alias für komplexe Typen.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Deklariert eine Klasse weder als `@struct` noch als `@dict` (dynamische Properties erlaubt).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},es:{blockDocs:{abstract:"Marca una clase o método como abstracto para la comprobación de tipos.\n",const:"Marca una variable/propiedad como de solo lectura (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Marca una función como constructor para usar con `new`.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Declara una constante de tiempo de compilación que puede sobrescribirse con `--define`.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Marca un símbolo como obsoleto; su uso genera una advertencia.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Declara un objeto tipo diccionario con propiedades dinámicas (acceso con corchetes).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Declara un tipo enum con un conjunto finito de valores.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Exporta/preserva el nombre de un símbolo (p. ej. con `--generate_exports`) y evita el renombrado.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Declara que una clase/interfaz extiende otro tipo.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Declara el archivo como externs.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"Bloque JSDoc a nivel de archivo (información y suppressions).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Impide extender una clase o sobrescribir un método.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Declara que un constructor implementa una interfaz.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Solo externs: permite asignar cualquier tipo, pero la lectura usa el tipo declarado.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Hereda la documentación del miembro sobrescrito (implica `@override`).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Marca una función como interfaz que describe miembros requeridos.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Marca una función como interfaz que describe miembros requeridos.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Trata las claves de un literal de objeto como propiedades de otro objeto.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Conserva un comentario de licencia/copyright en la salida.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Conserva un comentario de licencia/copyright en la salida.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Solo externs: declara que la llamada no tiene efectos secundarios; soporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Evita que una propiedad se colapse en una variable.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Analiza el archivo, pero no lo compila.\n\n```js\n/** @nocompile */\n```\n",noinline:"Experimental: evita optimizaciones de inlining.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Solo externs: declara que la llamada no tiene efectos secundarios; soporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Solo externs: declara que la llamada no tiene efectos secundarios; soporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Marca un miembro como override de un miembro de superclase/interfaz.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private: accesible solo desde el mismo directorio.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Documenta los tipos de parámetros.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Marca una clase como Polymer para el Polymer Pass.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Marca un objeto global como Polymer behavior.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Conserva un comentario de licencia/copyright en la salida.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Privado: accesible solo dentro del mismo archivo.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Protegido: accesible en subclases (+ mismo archivo).\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Público: miembro público (por defecto; rara vez explícito).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Marca una función como interfaz que describe miembros requeridos.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Documenta el tipo de retorno.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Declara un objeto de forma fija (acceso con punto, sin nuevas propiedades).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Suprime advertencias específicas del compilador.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Declara parámetros de tipo genéricos.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Especifica el tipo de `this` dentro de una función.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Documenta excepciones lanzadas (en externs se usa para análisis de efectos).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Declara el tipo de una variable/propiedad/expresión.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Crea un alias de tipo para tipos complejos.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Declara una clase ni `@struct` ni `@dict` (permite propiedades dinámicas).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},fr:{blockDocs:{abstract:"Marque une classe ou une méthode comme abstraite pour la vérification de types.\n",const:"Marque une variable/propriété en lecture seule (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Marque une fonction comme constructeur utilisé avec `new`.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Déclare une constante à la compilation, surchargeable via `--define`.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Marque un symbole comme obsolète ; l’utilisation déclenche un avertissement.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Déclare un objet de type dictionnaire avec des propriétés dynamiques (accès par crochets).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Déclare un type enum avec un ensemble fini de valeurs.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Exporte/préserve le nom d’un symbole (p. ex. avec `--generate_exports`) et évite le renommage.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Déclare qu’une classe/interface étend un autre type.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Déclare le fichier comme fichier externs.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"Bloc JSDoc au niveau du fichier (infos et suppressions).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Empêche l’extension d’une classe ou la redéfinition d’une méthode.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Déclare qu’un constructeur implémente une interface.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Externs uniquement : accepte toute affectation, mais lecture selon le type déclaré.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Hérite de la documentation du membre surchargé (implique `@override`).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Marque une fonction comme interface décrivant les membres requis.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Marque une fonction comme interface décrivant les membres requis.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Traite les clés d’un littéral d’objet comme propriétés d’un autre objet.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Préserve un commentaire de licence/copyright dans la sortie.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Préserve un commentaire de licence/copyright dans la sortie.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Externs uniquement : indique qu’un appel n’a pas d’effets de bord ; supporte `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Empêche une propriété d’être collée (collapse) en variable.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Analyse le fichier, mais saute la compilation.\n\n```js\n/** @nocompile */\n```\n",noinline:"Expérimental : empêche les optimisations d’inlining.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Externs uniquement : indique qu’un appel n’a pas d’effets de bord ; supporte `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Externs uniquement : indique qu’un appel n’a pas d’effets de bord ; supporte `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Marque un membre comme surchargeant un membre de superclasse/interface.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private : accessible uniquement depuis le même dossier.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Documente les types des paramètres.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Marque une classe comme Polymer pour le Polymer Pass.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Marque un objet global comme Polymer behavior.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Préserve un commentaire de licence/copyright dans la sortie.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Privé : accessible uniquement dans le même fichier.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Protégé : accessible dans les sous-classes (+ même fichier).\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Public : membre public (par défaut ; rarement explicite).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Marque une fonction comme interface décrivant les membres requis.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Documente le type de retour.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Déclare un objet à forme fixe (accès pointé, pas de nouvelles propriétés).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Supprime certaines catégories d’avertissements du compilateur.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Déclare des paramètres de type génériques.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Spécifie le type de `this` dans une fonction.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Documente les exceptions levées (en externs, utilisé pour l’analyse des effets).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Déclare le type d’une variable/propriété/expression.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Crée un alias de type pour des types complexes.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Déclare une classe ni `@struct` ni `@dict` (propriétés dynamiques autorisées).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},it:{blockDocs:{abstract:"Marca una classe o un metodo come astratto per la verifica dei tipi.\n",const:"Marca una variabile/proprietà come di sola lettura (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Marca una funzione come costruttore usato con `new`.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Dichiara una costante a compile-time, sovrascrivibile via `--define`.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Marca un simbolo come deprecato; l'uso genera un warning.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Dichiara un oggetto tipo dizionario con proprietà dinamiche (accesso con parentesi quadre).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Dichiara un tipo enum con un insieme finito di valori.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Esporta/preserva il nome di un simbolo (es. con `--generate_exports`) ed evita il rinominare.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Dichiara che una classe/interfaccia estende un altro tipo.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Dichiara il file come externs.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"Blocco JSDoc a livello file (info e suppressions).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Impedisce di estendere una classe o sovrascrivere un metodo.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Dichiara che un costruttore implementa un'interfaccia.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Solo externs: consente assegnazioni di qualsiasi tipo, ma la lettura è del tipo dichiarato.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Eredita la documentazione dal membro sovrascritto (implica `@override`).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Marca una funzione come interfaccia che descrive i membri richiesti.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Marca una funzione come interfaccia che descrive i membri richiesti.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Tratta le chiavi di un literal oggetto come proprietà di un altro oggetto.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Preserva un commento di licenza/copyright nell'output.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Preserva un commento di licenza/copyright nell'output.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Solo externs: dichiara che la chiamata non ha effetti collaterali; supporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Impedisce che una proprietà venga collassata in una variabile.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Esegue il parsing del file ma salta la compilazione.\n\n```js\n/** @nocompile */\n```\n",noinline:"Sperimentale: impedisce le ottimizzazioni di inlining.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Solo externs: dichiara che la chiamata non ha effetti collaterali; supporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Solo externs: dichiara che la chiamata non ha effetti collaterali; supporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Marca un membro come override di un membro della superclasse/interfaccia.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private: accessibile solo nella stessa directory.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Documenta i tipi dei parametri.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Marca una classe come Polymer per il Polymer Pass.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Marca un oggetto globale come Polymer behavior.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Preserva un commento di licenza/copyright nell'output.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Privato: accessibile solo nello stesso file.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Protetto: accessibile nelle sottoclassi (+ stesso file).\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Pubblico: membro pubblico (predefinito; raramente esplicito).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Marca una funzione come interfaccia che descrive i membri richiesti.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Documenta il tipo di ritorno.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Dichiara un oggetto a forma fissa (accesso con punto, niente nuove proprietà).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Sopprime specifici warning del compilatore.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Dichiara parametri di tipo generici.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Specifica il tipo di `this` dentro una funzione.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Documenta le eccezioni lanciate (in externs usato per l'analisi degli effetti).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Dichiara il tipo di una variabile/proprietà/espressione.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Crea un alias di tipo per tipi complessi.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Dichiara una classe né `@struct` né `@dict` (proprietà dinamiche consentite).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},ja:{blockDocs:{abstract:"@abstract JSDoc をサポートし、それを使って抽象クラス／抽象メソッドの意味論をコンパイル時にチェックできるようにすることは、長い間 Closure Compiler に対する機能要望でした。この機能は TypeScript など他の JavaScript 向けのオプショナル型システムにも存在します。Closure Compiler は 2016 年に、型システムレベルでクラスおよびメソッドに対する @abstract サポートを追加しました。新しい JavaScript コードでは、goog.abstractMethod よりも @abstract JSDoc を使用することが推奨されます。",const:"変数を読み取り専用としてマークします。コンパイラは `@const` 変数をインライン化できるため、JavaScript の最適化に役立ちます。  \n型宣言は任意です。  \n`@const` が付いた変数に複数回値を代入すると、コンパイラは警告を出します。変数がオブジェクトの場合、オブジェクトのプロパティ変更までは禁止されない点に注意してください。  \n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n`@const` 注釈は、インターフェース／レコード／クラス／enum へのエイリアスを注釈する方法でもあります。この注釈がないと、そのエイリアス記号は型位置で認識されません。  \n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n元の注釈（例: `@enum`）をエイリアス宣言側で繰り返すことは許可されません。  \n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"関数をコンストラクタとしてマークします。コンパイラは `new` キーワードで使われる関数に対して `@constructor` 注釈を要求します。EcmaScript の `class` のコンストラクタメソッドや `goog.defineClass` のコンストラクタメソッドには `@constructor` を付けないでください。  \n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"コンパイル時にコンパイラによって上書きできる定数を示します。左の例では、`--define='ENABLE_DEBUG=false'` というフラグを渡すことで `ENABLE_DEBUG` の値を `false` に変更できます。define できる定数の型は `number` / `string` / `boolean` のいずれかです。define はグローバルスコープでのみ許可されます。  \n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"マークされた関数・メソッド・プロパティが非推奨であることを示します。非推奨メソッドを使用するとコンパイラは警告を出します。  \n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"`@dict` は可変数のプロパティを持つオブジェクトを作成するために使います。コンストラクタ（例では Foo）に `@dict` を付けると、Foo オブジェクトのプロパティにはドット記法ではなくブラケット記法でのみアクセスできます。この注釈はオブジェクトリテラルに直接書くこともできます。  \n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\nまたは class キーワードを使う場合:  \n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"列挙型 (enum) を指定します。enum は、取り得る値が有限個（多くは文字列または数値）である型です。`@enum` タグの後には型式 (type expression) が必要です。enum の型を省略した場合は `number` が仮定されます。  \nenum の型ラベルは、その enum の各プロパティにも適用されます。たとえば enum の型が `number` なら、列挙される各プロパティも `number` でなければなりません。  \n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"static または prototype プロパティに `@export` を付けて、コンパイラを `--generate_exports` フラグ付きで実行すると、対応する `goog.exportSymbol` 文が生成されます。  \n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n`/** @export {SomeType} */` は `/** @export @type {SomeType} */` の短縮形として書けます。  \nインスタンスプロパティに `@export` を付けると、コンパイラはそのプロパティについて（同一コンパイル単位内で）名前変更（property renaming）を控えるようになります。  \n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\nプロパティ名がリネームされた後は次のようになります。  \n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n@export 注釈を使うコードは、次のいずれかを満たす必要があります。  \n\n<li>`closure/base.js` を含める</li>\n<li>自分のコードベース内で `goog.exportSymbol` と `goog.exportProperty` を同じシグネチャで定義する</li>\n  \n",extends:"クラスまたはインターフェースが別のクラスを継承していることを示します。`@extends` を付けたクラスは、`@constructor` / `@interface` / `@record` のいずれかも付ける必要があります。  \n\n注意: `@extends` はクラスを実際に継承させるわけではありません。この注釈は「型チェック時に、あるクラスを別のクラスのサブクラスとして扱ってよい」ことをコンパイラに伝えるだけです。\n  \n継承の実装例については、Closure Library の関数 `goog.inherits()` を参照してください。  \n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n`@constructor` では `@extends` は 1 つだけ許可されますが、`@interface` または `@record` では複数指定できます。  \n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n\n注意: `class` 式では `@extends` は暗黙ですが、たとえばスーパークラス型に型引数を与えたい場合など、明示的に書くこともできます。\n  \n",externs:"externs ファイルを宣言します。  \n@fileoverview ブロック内で @externs が付いたファイルは、通常の src としてコンパイル単位に含まれていても externs ファイルとして扱われます。  \n例:  \n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"コメントブロックにファイルレベルの情報（suppress など）を持たせます。  \n例:  \n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"クラスに付けると「このクラスは継承してはいけない」ことを示します。メソッドに付けると「サブクラスはこのメソッドを override してはいけない」ことを示します。  \n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"`@constructor` と併用して、クラスがインターフェースを実装することを示します。  \nコンパイラは、コンストラクタに `@implements` を付けたのに、そのインターフェースが要求するメソッドやプロパティを実装していない場合に警告を出します。  \n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"この注釈は externs のプロパティ宣言にのみ書けます。プロパティには宣言型があるものの、任意の型を代入しても警告されません。プロパティにアクセスすると、宣言型の値として取得できます。たとえば `element.innerHTML` は任意の型を代入できますが、常に文字列を返します。  \n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"サブクラスのメソッドまたはプロパティが、スーパークラスのメソッドまたはプロパティを意図的に隠し、かつドキュメントが完全に同一であることを示します。`@inheritDoc` タグは `@override` タグを含意します。`@override` の使用が推奨です。  \n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\nクラスがスーパークラスを継承している、またはインターフェースを実装している場合、このタグは不要です（その場合はドキュメントが自動的に継承されます）。このタグは主に、スーパークラス側のメソッド名が `foobar1` なのにサブクラス側で誤って `foobar2` と書いてしまった、のようなタイポ検出に役立ちます。  \n",interface:"関数をインターフェースとしてマークします。インターフェースは型に必要なメンバーを規定します。インターフェースを実装するクラスは、そのプロトタイプ上で定義されたメソッドやプロパティをすべて実装する必要があります。`@implements` も参照してください。  \nコンパイラは、インターフェースがインスタンス化されないことを検証します。インターフェース関数に対して `new` キーワードが使われると警告を出します。  \n@record と @interface の違いについては [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler) を参照してください。  \n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"オブジェクトリテラルのキーを、別のオブジェクトのプロパティとして扱うことを示します。この注釈はオブジェクトリテラルにのみ書いてください。  \n波括弧内の名前は、他の注釈のような型名ではなく「オブジェクト名」です。これはプロパティを貸し出す先のオブジェクトを指します。たとえば `@type {Foo}` は「Foo のインスタンス」を意味しますが、`@lends {Foo}` は「コンストラクタ Foo」を意味します。  \nJSDoc Toolkit のドキュメントにも、この注釈の説明があります。  \n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"マークされたファイルのコンパイル結果の先頭に、関連するコメントを挿入するようコンパイラに指示します。この注釈により、重要な通知（法的なライセンスや著作権テキストなど）をコンパイル後もそのまま残せます。改行は保持されます。  \n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"`@nosideeffects` は、宣言された関数呼び出しに副作用がないことを示します。この注釈により、戻り値が使われていない場合にコンパイラはその関数呼び出しを削除できます。これは関数が「純粋 (pure)」であることを示すシグナルではありません。関数が可変なグローバル状態を読む可能性はあります。  \n`@modifies {this}` は、指定した `this` の直接プロパティだけが変更されることを示します。これらの関数呼び出しは、`this` オブジェクトが他に使われないことが分かっている場合に削除されることがあります。  \n`@modifies {arguments}` は、指定した引数の直接プロパティだけが変更されることを示します。これらの関数呼び出しは、渡されたパラメータが未使用であることが分かっている場合に削除されることがあります。  \nこれらの注釈は extern ファイルでのみ使用できます。  \n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:'externs ファイル内で、コンパイラの "alias externals" パスの一部として変数や関数をエイリアスしないよう指示します。この機能はデフォルトでは有効になっておらず、Java API 経由でのみ利用できます。  \n例:  \n```js\n/** @noalias */\nfunction Range() {}\n```\n',nocollapse:"プロパティがコンパイラによって変数へ畳み込まれないようにします。`@nocollapse` の主な用途は、可変なプロパティを export できるようにすることです。なお、畳み込まれないプロパティでもコンパイラによって名前変更（renaming）され得ます。オブジェクトに `@nocollapse` を付けた場合、そのプロパティ配下もすべて畳み込まれません。  \n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"例:  \n```js\n/** @nocompile */\n```\nファイルの先頭で使い、コンパイラに「このファイルはパースするがコンパイルしない」ことを伝えます。コンパイル対象ではないコード（ブートストラップコードなど）をコンパイルテストから除外したい場合に使います。多くの場合は、`@define` など別の手段で挙動を切り替えるのがよいでしょう。  \n",noinline:"(EXPERIMENTAL) 最適化で関数や変数をインライン化しないことを示します。  \n現在、この注釈は一般的なケースでは動作しますが、インライン化が発生してしまう別経路がコンパイラ内に存在する可能性があります。背景については [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) を参照し、別のユースケースがある場合は feature request を作成してください。  \n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"`@nosideeffects` は、宣言された関数呼び出しに副作用がないことを示します。この注釈により、戻り値が使われていない場合にコンパイラはその関数呼び出しを削除できます。これは関数が「純粋 (pure)」であることを示すシグナルではありません。関数が可変なグローバル状態を読む可能性はあります。  \n`@modifies {this}` は、指定した `this` の直接プロパティだけが変更されることを示します。これらの関数呼び出しは、`this` オブジェクトが他に使われないことが分かっている場合に削除されることがあります。  \n`@modifies {arguments}` は、指定した引数の直接プロパティだけが変更されることを示します。これらの関数呼び出しは、渡されたパラメータが未使用であることが分かっている場合に削除されることがあります。  \nこれらの注釈は extern ファイルでのみ使用できます。  \n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"メソッドまたはプロパティが「クラスが実装しているインターフェースの一部である」か、または「スーパークラスのメソッド／プロパティを意図的に隠している」ことを示します。他の注釈が付いていない場合、このメソッド／プロパティはスーパークラスから注釈を自動的に継承します。  \n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n`@override` タグの有無は、スーパークラスを拡張するクラスやインターフェースを実装するクラス内のメソッドについて、コンパイラが推論する型に影響しません。これらの場合、ドキュメントは常に overridden なメソッドから自動継承されます。とはいえ、推奨コンパイラフラグセットでは `@override` の欠落や不要な付与に対して警告が出ます。これは、スーパークラス側が `foobar1` なのにサブクラス側で `foobar2` としてしまう、といったタイポ検出に主に有効です。  \n",package:"メンバーまたはプロパティを package private としてマークします。`@package` が付いた名前にアクセスできるのは同じディレクトリ内のコードだけです。特に、親ディレクトリや子ディレクトリのコードはアクセスできません。  \npublic コンストラクタに `@package` プロパティを持たせると、ディレクトリ外の呼び出し側が使えるメソッドを制限できます。一方で、`@package` コンストラクタに public プロパティを持たせると、ディレクトリ外の呼び出し側が型を直接インスタンス化できないようにできます。  \n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"メソッド / 関数 / コンストラクタ定義で、引数の型を指定します。  \n`@param` タグの後には型式 (type expression) が必要です。  \n別の方法として、引数の型をインラインで注釈できます（例の `foo` 関数を参照）。  \n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"`class` が Polymer セマンティクスを持つことを示します。`--polymer_pass=2` フラグが指定されている場合にのみ、コンパイラへ影響します。[Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) が Polymer クラスを認識し、適切な型情報を追加できるようにします。  \n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"グローバルオブジェクトが Polymer behavior として利用できることを示します。[Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) が behavior を認識し、利用側の Polymer 定義へ型情報をコピーできるようにします。behavior オブジェクトはグローバルである必要があります。  \n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"マークされたファイルのコンパイル結果の先頭に、関連するコメントを挿入するようコンパイラに指示します。この注釈により、重要な通知（法的なライセンスや著作権テキストなど）をコンパイル後もそのまま残せます。改行は保持されます。  \n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"メンバーを private としてマークします。`@private` が付いたグローバル変数や関数にアクセスできるのは同一ファイル内のコードだけです。`@private` が付いたコンストラクタは、同一ファイル内のコード、およびその static / instance メンバーからのみインスタンス化できます。  \nまた、`@private` コンストラクタの public static プロパティはどこからでもアクセスできます。  \n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"メンバーまたはプロパティが protected であることを示します。`@protected` が付いたプロパティにアクセスできるのは次の範囲です。  \n\n<li>同一ファイル内のすべてのコード</li>\n<li>そのプロパティが定義されたクラスの任意のサブクラスの static メソッドおよび instance メソッド</li>\n  \n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"メンバーまたはプロパティが public であることを示します。`@public` が付いたプロパティは、どのファイルからでもアクセスできます。これは暗黙のデフォルトであり、通常はほとんど使いません。難読化ビルドで名前を保持することを意味するものではありません（`@export` を参照）。  \n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"関数をインターフェースとしてマークします。インターフェースは型に必要なメンバーを規定します。インターフェースを実装するクラスは、そのプロトタイプ上で定義されたメソッドやプロパティをすべて実装する必要があります。`@implements` も参照してください。  \nコンパイラは、インターフェースがインスタンス化されないことを検証します。インターフェース関数に対して `new` キーワードが使われると警告を出します。  \n@record と @interface の違いについては [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler) を参照してください。  \n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"メソッド / 関数定義の戻り値の型を指定します。`@return` タグの後には型式 (type expression) が必要です。  \n別の方法として、戻り値の型をインラインで注釈できます（例の `foo` 関数を参照）。  \nexterns ではない関数が戻り値を持たない場合、`@return` タグを省略でき、その場合コンパイラは戻り値を `undefined` と仮定します。  \n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"`@struct` は固定数のプロパティを持つオブジェクトを作成するために使います。コンストラクタ（例では Foo）に `@struct` を付けると、Foo オブジェクトのプロパティにはブラケット記法ではなくドット記法でのみアクセスできます。また、Foo インスタンス生成後にプロパティを追加できません。この注釈はオブジェクトリテラルに直接書くこともできます。  \nES の class キーワードや `goog.defineClass` で作られたクラスはデフォルトで `@struct` なので、この注釈が必要なのは古いスタイルの `@constructor` 関数だけです。  \n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"警告を抑制します。警告カテゴリは `|` または `,` で区切ります。\n警告名の一覧は [Warnings](https://github.com/google/closure-compiler/wiki/Warnings) を参照してください。  \n例:  \n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"[Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types) を参照してください。  \n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"関数内で `this` キーワードが指すオブジェクトの型を指定します。`@this` タグの後には型式 (type expression) が必要です。  \nコンパイラ警告を避けるため、`this` が「プロトタイプメソッドでも `@constructor` でもない関数」に出現する場合は、必ず `@this` 注釈を付ける必要があります。  \n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"関数が投げる例外をドキュメント化します。型チェッカーは現時点でこの情報を利用しません。これは externs ファイルで宣言された関数が副作用を持つかどうかを判定するためにのみ利用されます。  \n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"変数・プロパティ・式の型を指定します。`@type` タグの後には型式 (type expression) が必要です。2 つ目の例のように、型注釈をインラインで書いて `@type` を省略することもできます。  \n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"複雑な型（union や record 型を含む）は、利便性と保守性のために typedef で別名を付けられます。これらの注釈は長くなりがちですが、読みやすさのために複数行に分割できます。  \n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'クラスが `@struct` 型でも `@dict` 型でもないことを示します。これは、`@constructor` 関数で作られた古い Closure スタイルのクラスにおけるデフォルトです。`goog.defineClass` と ES の class キーワードはデフォルトで `@struct` なクラスを生成するため、必要に応じて明示的に `@unrestricted` を付ける必要があります。  \n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\nまたは class キーワードを使う場合:  \n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},ko:{blockDocs:{abstract:"클래스/메서드를 추상(abstract)으로 표시하여 타입 체크에 활용합니다.\n",const:"변수/프로퍼티를 읽기 전용으로 표시합니다(@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"`new`와 함께 쓰는 생성자 함수임을 표시합니다.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"`--define`으로 컴파일 타임에 덮어쓸 수 있는 상수를 선언합니다.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"심볼을 deprecated로 표시하며 사용 시 경고가 발생합니다.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"동적 프로퍼티를 가진 딕셔너리 형태 객체를 선언합니다(대괄호 접근).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"유한한 값 집합을 갖는 enum 타입을 선언합니다.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"심볼 이름을 export/보존합니다(예: `--generate_exports`) 및 이름 변경을 방지합니다.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"클래스/인터페이스가 다른 타입을 확장함을 선언합니다.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"파일을 externs 파일로 선언합니다.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"파일 수준의 JSDoc 블록(파일 정보 및 suppression).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"클래스 확장 또는 메서드 오버라이드를 금지합니다.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"생성자가 인터페이스를 구현함을 선언합니다.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"externs 전용: 선언된 타입으로 읽히지만 어떤 타입도 대입할 수 있습니다.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"오버라이드한 멤버의 문서를 상속합니다(`@override` 포함).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"필요한 멤버를 정의하는 인터페이스로 함수를 표시합니다.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"필요한 멤버를 정의하는 인터페이스로 함수를 표시합니다.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"객체 리터럴의 키를 다른 객체의 프로퍼티로 취급합니다.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"라이선스/저작권 주석을 컴파일 결과에 그대로 보존합니다.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"라이선스/저작권 주석을 컴파일 결과에 그대로 보존합니다.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"externs 전용: 호출에 부작용이 없음을 선언하며 `@modifies`를 지원합니다.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"프로퍼티가 변수로 collapse(병합)되는 것을 방지합니다.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"파일을 파싱하지만 컴파일은 생략합니다.\n\n```js\n/** @nocompile */\n```\n",noinline:"실험적: 인라인(inlining) 최적화를 방지합니다.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"externs 전용: 호출에 부작용이 없음을 선언하며 `@modifies`를 지원합니다.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"externs 전용: 호출에 부작용이 없음을 선언하며 `@modifies`를 지원합니다.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"멤버가 상위 클래스/인터페이스 멤버를 오버라이드함을 표시합니다.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"패키지 프라이빗: 같은 디렉터리에서만 접근 가능합니다.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"함수/메서드의 매개변수 타입을 문서화합니다.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Polymer Pass를 위해 클래스가 Polymer 의미론을 가짐을 표시합니다.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"전역 객체를 Polymer behavior로 표시합니다.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"라이선스/저작권 주석을 컴파일 결과에 그대로 보존합니다.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"프라이빗: 같은 파일 내부에서만 접근 가능합니다.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"프로텍티드: 서브클래스(+ 같은 파일)에서 접근 가능합니다.\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"퍼블릭: 공개 멤버(기본값, 드물게 명시).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"필요한 멤버를 정의하는 인터페이스로 함수를 표시합니다.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"함수/메서드의 반환 타입을 문서화합니다.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"고정된 형태의 객체를 선언합니다(점 접근, 새 프로퍼티 추가 불가).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"특정 컴파일러 경고를 억제합니다.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"제네릭 타입 파라미터를 선언합니다.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"함수 내부의 `this` 타입을 지정합니다.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"던질 수 있는 예외를 문서화합니다(externs에서 부작용 분석에 사용).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"변수/프로퍼티/표현식의 타입을 선언합니다.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"복잡한 타입을 위한 타입 별칭을 만듭니다.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'`@struct`도 `@dict`도 아님을 선언하며 동적 프로퍼티를 허용합니다.\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},pl:{blockDocs:{abstract:"Oznacza klasę lub metodę jako abstrakcyjną dla potrzeb kontroli typów.\n",const:"Oznacza zmienną/właściwość jako tylko do odczytu (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Oznacza funkcję jako konstruktor używany z `new`.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Deklaruje stałą kompilacji, którą można nadpisać przez `--define`.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Oznacza symbol jako przestarzały; użycie wywołuje ostrzeżenie.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Deklaruje obiekt słownikowy z dynamicznymi właściwościami (dostęp przez nawiasy).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Deklaruje typ enum o skończonym zbiorze wartości.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Eksportuje/zachowuje nazwę symbolu (np. z `--generate_exports`) i zapobiega zmianie nazwy.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Deklaruje, że klasa/interfejs rozszerza inny typ.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Deklaruje plik jako externs.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"Blok JSDoc na poziomie pliku (informacje i suppressions).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Zabrania dziedziczenia klasy lub nadpisywania metody.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Deklaruje, że konstruktor implementuje interfejs.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Tylko externs: pozwala przypisać dowolny typ, ale odczyt ma typ zadeklarowany.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Dziedziczy dokumentację z nadpisywanego elementu (implikuje `@override`).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Oznacza funkcję jako interfejs opisujący wymagane elementy.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Oznacza funkcję jako interfejs opisujący wymagane elementy.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Traktuje klucze literału obiektu jako właściwości innego obiektu.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Zachowuje komentarz licencyjny/praw autorskich w wyniku kompilacji.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Zachowuje komentarz licencyjny/praw autorskich w wyniku kompilacji.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Tylko externs: deklaruje brak efektów ubocznych; wspiera `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Zapobiega collapse właściwości do zmiennej.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Plik jest parsowany, ale nie jest kompilowany.\n\n```js\n/** @nocompile */\n```\n",noinline:"Eksperymentalne: blokuje optymalizacje inliningu.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Tylko externs: deklaruje brak efektów ubocznych; wspiera `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Tylko externs: deklaruje brak efektów ubocznych; wspiera `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Oznacza, że element nadpisuje element z nadklasy/interfejsu.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private: dostępny tylko w tym samym katalogu.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Dokumentuje typy parametrów funkcji/metody.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Oznacza klasę jako Polymer dla Polymer Pass.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Oznacza globalny obiekt jako Polymer behavior.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Zachowuje komentarz licencyjny/praw autorskich w wyniku kompilacji.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Prywatne: dostępne tylko w tym samym pliku.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Chronione: dostępne w podklasach (+ w tym samym pliku).\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Publiczne: element publiczny (domyślnie; rzadko jawnie).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Oznacza funkcję jako interfejs opisujący wymagane elementy.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Dokumentuje typ zwracany.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Deklaruje obiekt o stałym kształcie (dostęp kropką, bez nowych właściwości).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Wyłącza wybrane ostrzeżenia kompilatora.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Deklaruje generyczne parametry typów.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Określa typ `this` wewnątrz funkcji.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Dokumentuje rzucane wyjątki (w externs używane do analizy efektów).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Deklaruje typ zmiennej/właściwości/wyrażenia.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Tworzy alias typu dla złożonych typów.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Deklaruje klasę jako ani `@struct` ani `@dict` (dozwolone dynamiczne właściwości).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},"pt-br":{blockDocs:{abstract:"Marca uma classe ou método como abstrato para checagem de tipos.\n",const:"Marca uma variável/propriedade como somente leitura (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Marca uma função como construtor usado com `new`.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Declara uma constante de compilação que pode ser sobrescrita via `--define`.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Marca um símbolo como deprecated; o uso gera um aviso.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Declara um objeto tipo dicionário com propriedades dinâmicas (acesso por colchetes).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Declara um tipo enum com um conjunto finito de valores.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Exporta/preserva o nome de um símbolo (ex.: `--generate_exports`) e evita renomeação.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Declara que uma classe/interface estende outro tipo.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Declara o arquivo como externs.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"Bloco JSDoc em nível de arquivo (informações e suppressions).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Impede estender uma classe ou sobrescrever um método.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Declara que um construtor implementa uma interface.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Somente externs: permite atribuir qualquer tipo, mas a leitura segue o tipo declarado.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Herda a documentação do membro sobrescrito (implica `@override`).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Marca uma função como interface descrevendo membros obrigatórios.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Marca uma função como interface descrevendo membros obrigatórios.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Trata as chaves de um literal de objeto como propriedades de outro objeto.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Preserva um comentário de licença/copyright no output compilado.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Preserva um comentário de licença/copyright no output compilado.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Somente externs: declara que a chamada não tem efeitos colaterais; suporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Impede que uma propriedade seja colapsada em uma variável.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Faz o parse do arquivo, mas não o compila.\n\n```js\n/** @nocompile */\n```\n",noinline:"Experimental: impede otimizações de inlining.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Somente externs: declara que a chamada não tem efeitos colaterais; suporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Somente externs: declara que a chamada não tem efeitos colaterais; suporta `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Marca um membro como override de um membro da superclasse/interface.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private: acessível apenas no mesmo diretório.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Documenta os tipos de parâmetros.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Marca uma classe como Polymer para o Polymer Pass.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Marca um objeto global como Polymer behavior.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Preserva um comentário de licença/copyright no output compilado.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Privado: acessível apenas no mesmo arquivo.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Protegido: acessível em subclasses (+ mesmo arquivo).\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Público: membro público (padrão; raramente explícito).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Marca uma função como interface descrevendo membros obrigatórios.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Documenta o tipo de retorno.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Declara um objeto de forma fixa (acesso por ponto, sem novas propriedades).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Suprime categorias específicas de avisos.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Declara parâmetros de tipo genéricos.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Especifica o tipo de `this` dentro de uma função.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Documenta exceções lançadas (em externs usado na análise de efeitos).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Declara o tipo de uma variável/propriedade/expressão.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Cria um alias de tipo para tipos complexos.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Declara uma classe nem `@struct` nem `@dict` (permite propriedades dinâmicas).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},ru:{blockDocs:{abstract:"Помечает класс или метод как абстрактный для проверки типов.\n",const:"Помечает переменную/свойство как только для чтения (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Помечает функцию как конструктор, используемый с `new`.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Объявляет compile-time константу, которую можно переопределить через `--define`.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Помечает символ как устаревший; использование вызывает предупреждение.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Объявляет словарный объект с динамическими свойствами (доступ через скобки).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Объявляет enum-тип с конечным набором значений.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Экспортирует/сохраняет имя символа (например, с `--generate_exports`) и предотвращает переименование.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Объявляет, что класс/интерфейс расширяет другой тип.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Объявляет файл как externs.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"JSDoc-блок на уровне файла (информация и suppressions).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Запрещает наследование класса или переопределение метода.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Объявляет, что конструктор реализует интерфейс.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Только externs: разрешает присваивать любой тип, но чтение идёт как объявленный тип.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Наследует документацию у переопределяемого члена (подразумевает `@override`).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Помечает функцию как интерфейс, задающий обязательные члены.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Помечает функцию как интерфейс, задающий обязательные члены.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Считает ключи object literal свойствами другого объекта.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Сохраняет комментарий лицензии/копирайта в скомпилированном результате.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Сохраняет комментарий лицензии/копирайта в скомпилированном результате.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Только externs: объявляет отсутствие побочных эффектов; поддерживает `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Запрещает схлопывание свойства в переменную.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Парсит файл, но пропускает компиляцию.\n\n```js\n/** @nocompile */\n```\n",noinline:"Экспериментально: запрещает inlining-оптимизации.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Только externs: объявляет отсутствие побочных эффектов; поддерживает `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Только externs: объявляет отсутствие побочных эффектов; поддерживает `@modifies`.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Помечает член как переопределяющий член суперкласса/интерфейса.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private: доступно только из того же каталога.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Документирует типы параметров.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Помечает класс как Polymer для Polymer Pass.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Помечает глобальный объект как Polymer behavior.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Сохраняет комментарий лицензии/копирайта в скомпилированном результате.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Приватный: доступен только в пределах файла.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Защищённый: доступен в подклассах (+ в том же файле).\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Публичный: публичный член (по умолчанию; редко явно).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Помечает функцию как интерфейс, задающий обязательные члены.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Документирует возвращаемый тип.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Объявляет объект фиксированной формы (доступ точкой, без новых свойств).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Подавляет указанные предупреждения компилятора.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Объявляет обобщённые (generic) параметры типов.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Указывает тип `this` внутри функции.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Документирует выбрасываемые исключения (в externs используется для анализа побочных эффектов).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Объявляет тип переменной/свойства/выражения.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Создаёт алиас типа для сложных типов.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Объявляет класс ни `@struct`, ни `@dict` (разрешает динамические свойства).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},tr:{blockDocs:{abstract:"Bir sinifi veya metodu soyut (abstract) olarak isaretler ve tur denetimine yardimci olur.\n",const:"Bir degiskeni/ozelligi salt okunur olarak isaretler (@const).\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"`new` ile kullanilan bir kurucu fonksiyon oldugunu belirtir.\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"`--define` ile derleme zamaninda degistirilebilen bir sabit tanimlar.\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Bir sembolu deprecated olarak isaretler; kullanim uyarisi uretir.\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"Dinamik ozelliklere sahip sozluk benzeri nesne tanimlar (koseli parantez erisimi).\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Sonlu deger kumesine sahip bir enum turu tanimlar.\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"Sembol adini disa aktarir/korur (orn. `--generate_exports`) ve yeniden adlandirmayi engeller.\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"Bir sinifin/arayuzun baska bir turu genislettigini belirtir.\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"Dosyayi externs dosyasi olarak belirtir.\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"Dosya duzeyinde JSDoc blogu (dosya bilgisi ve suppression).\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"Sinifin genisletilmesini veya metodun override edilmesini engeller.\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Bir kurucunun bir arayuzu uyguladigini belirtir.\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"Yalnizca externs: herhangi bir tur atanabilir, ancak okuma bildirilen tur olarak yapilir.\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Override edilen uyeden dokumantasyonu devralir (`@override` icerir).\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"Gerekli uyeleri tanimlayan bir arayuz olarak fonksiyonu isaretler.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"Gerekli uyeleri tanimlayan bir arayuz olarak fonksiyonu isaretler.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"Nesne literal'inin anahtarlarini baska bir nesnenin ozellikleri gibi ele alir.\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"Lisans/telif bildirimi yorumunu ciktiya korur.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"Lisans/telif bildirimi yorumunu ciktiya korur.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"Yalnizca externs: cagriyi yan etkisiz olarak belirtir; `@modifies` destekler.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"Bir ozelligin degiskene collapse edilmesini engeller.\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"Dosyayi parse eder ama derlemez.\n\n```js\n/** @nocompile */\n```\n",noinline:"Deneysel: inlining optimizasyonlarini engeller.\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"Yalnizca externs: cagriyi yan etkisiz olarak belirtir; `@modifies` destekler.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"Yalnizca externs: cagriyi yan etkisiz olarak belirtir; `@modifies` destekler.\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"Bir uyenin ust sinif/arayuz uyesini override ettigini belirtir.\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"Package-private: yalnizca ayni dizinden erisilebilir.\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Parametre turlerini belgelendirir.\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Polymer Pass icin sinifi Polymer olarak isaretler.\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Global nesneyi Polymer behavior olarak isaretler.\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Lisans/telif bildirimi yorumunu ciktiya korur.\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Ozel: yalnizca ayni dosyada erisilebilir.\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Korumali: alt siniflarda (+ ayni dosyada) erisilebilir.\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Genel: public uye (varsayilan; nadiren acikca).\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Gerekli uyeleri tanimlayan bir arayuz olarak fonksiyonu isaretler.\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Donus turunu belgelendirir.\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"Sabit sekilli nesne tanimlar (nokta erisimi, yeni ozellik yok).\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Belirli derleyici uyarilarini bastirir.\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"Generic tur parametrelerini tanimlar.\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Fonksiyon icinde `this` turunu belirtir.\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Atilan istisnalari belgelendirir (externs'te yan etki analizi icin kullanilir).\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Bir degiskenin/ozelligin/ifadenin turunu bildirir.\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Karmasik turler icin tur takma adi olusturur.\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Sinifin ne `@struct` ne de `@dict` oldugunu belirtir (dinamik ozelliklere izin verir).\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},"zh-cn":{blockDocs:{abstract:"将类或方法标记为抽象，用于类型检查。\n",const:"将变量/属性标记为只读（@const）。\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"将函数标记为构造函数，用于 `new`。\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"声明可通过 `--define` 在编译期覆盖的常量。\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"标记为已弃用；使用时会产生编译器警告。\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"声明具有动态属性的字典对象（使用方括号访问）。\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"声明具有有限取值集合的枚举类型。\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"导出/保留符号名称（例如配合 `--generate_exports`），并避免重命名。\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"声明类/接口继承（extends）另一个类型。\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"将文件声明为 externs 文件。\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"文件级 JSDoc 块（文件信息与警告抑制）。\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"禁止类被继承或方法被重写。\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"声明构造函数实现某个接口。\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"仅 externs：可赋任意类型，但读取时按声明类型处理。\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"继承被重写成员的文档（隐含 `@override`）。\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"将函数标记为接口，用于描述必须实现的成员。\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"将函数标记为接口，用于描述必须实现的成员。\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"将对象字面量的键视为另一个对象的属性。\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"在输出中保留许可证/版权声明注释。\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"在输出中保留许可证/版权声明注释。\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"仅 externs：声明调用无副作用，并支持 `@modifies`。\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"防止属性被折叠（collapse）成变量。\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"解析文件但跳过编译。\n\n```js\n/** @nocompile */\n```\n",noinline:"实验性：禁止内联（inlining）优化。\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"仅 externs：声明调用无副作用，并支持 `@modifies`。\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"仅 externs：声明调用无副作用，并支持 `@modifies`。\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"标记成员重写了父类/接口的成员。\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"包私有：仅同一目录可访问。\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"文档化函数/方法的参数类型。\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"标记类具有 Polymer 语义（Polymer Pass）。\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"将全局对象标记为 Polymer behavior。\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"在输出中保留许可证/版权声明注释。\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"私有：仅同一文件可访问。\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"受保护：子类（以及同一文件）可访问。\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"公有：公开成员（默认；很少显式标注）。\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"将函数标记为接口，用于描述必须实现的成员。\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"文档化返回值类型。\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"声明固定形状对象（点访问，不可新增属性）。\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"抑制指定的编译器警告。\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"声明泛型类型参数。\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"指定函数内部 `this` 的类型。\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"文档化可能抛出的异常（externs 中用于副作用分析）。\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"声明变量/属性/表达式的类型。\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"为复杂类型创建类型别名。\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'声明类既不是 `@struct` 也不是 `@dict`（允许动态属性）。\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i},"zh-tw":{blockDocs:{abstract:"將類別或方法標記為抽象，用於型別檢查。\n",const:"將變數/屬性標記為唯讀（@const）。\n\n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\n\n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\n\n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"將函式標記為建構子，用於 `new`。\n\n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"宣告可透過 `--define` 在編譯期覆寫的常數。\n\n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"標記為已棄用；使用時會產生編譯器警告。\n\n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"宣告具有動態屬性的字典物件（使用方括號存取）。\n\n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n\n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"宣告具有有限取值集合的列舉型別。\n\n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"匯出/保留符號名稱（例如搭配 `--generate_exports`），並避免重新命名。\n\n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\n\n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\n\n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\n",extends:"宣告類別/介面繼承（extends）另一個型別。\n\n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\n\n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n",externs:"將檔案宣告為 externs 檔。\n\n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"檔案層級的 JSDoc 區塊（檔案資訊與警告抑制）。\n\n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"禁止類別被繼承或方法被覆寫。\n\n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"宣告建構子實作某個介面。\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"僅 externs：可指派任何型別，但讀取時按宣告型別處理。\n\n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"繼承被覆寫成員的文件（隱含 `@override`）。\n\n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",interface:"將函式標記為介面，用於描述必須實作的成員。\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n","interface:record":"將函式標記為介面，用於描述必須實作的成員。\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:"將物件字面量的鍵視為另一個物件的屬性。\n\n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n",license:"在輸出中保留授權/版權聲明註解。\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n","license:preserve":"在輸出中保留授權/版權聲明註解。\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:"僅 externs：宣告呼叫無副作用，並支援 `@modifies`。\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",noalias:"@noalias\n\n```js\n/** @noalias */\nfunction Range() {}\n```\n",nocollapse:"防止屬性被折疊（collapse）成變數。\n\n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"解析檔案但跳過編譯。\n\n```js\n/** @nocompile */\n```\n",noinline:"實驗性：禁止內聯（inlining）最佳化。\n\nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.\n\n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:"僅 externs：宣告呼叫無副作用，並支援 `@modifies`。\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n","nosideeffects:modifies":"僅 externs：宣告呼叫無副作用，並支援 `@modifies`。\n\n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n",override:"標記成員覆寫了父類別/介面的成員。\n\n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\n",package:"套件私有：僅同一目錄可存取。\n\n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"文件化函式/方法的參數型別。\n\n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"標記類別具有 Polymer 語義（Polymer Pass）。\n\nIndicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.\n\n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"將全域物件標記為 Polymer behavior。\n\nIndicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.\n\n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"在輸出中保留授權/版權聲明註解。\n\n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"私有：僅同一檔案可存取。\n\n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"受保護：子類別（以及同一檔案）可存取。\n\n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"公有：公開成員（預設；很少顯式標註）。\n\n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"將函式標記為介面，用於描述必須實作的成員。\n\nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)\n\n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"文件化回傳值型別。\n\n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"宣告固定形狀物件（點存取，不可新增屬性）。\n\n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"抑制指定的編譯器警告。\n\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).\n\n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"宣告泛型型別參數。\n\nSee [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).\n\n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"指定函式內 `this` 的型別。\n\n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"文件化可能拋出的例外（externs 中用於副作用分析）。\n\n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"宣告變數/屬性/表達式的型別。\n\n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"為複雜型別建立型別別名。\n\n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'宣告類別既不是 `@struct` 也不是 `@dict`（允許動態屬性）。\n\n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n\n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:i}}},28(n,e){Object.defineProperty(e,"__esModule",{value:!0}),e.closurePresetDocsTranslations=void 0,e.closurePresetDocsTranslations={en:{blockDocs:{abstract:"Supporting the @abstract JsDoc and using that to aid compile-time checks on the semantics of abstract classes and methods was a feature request to Closure Compiler for a long time. This feature is also present in some other JavaScript optional type systems, such as TypeScript.  Closure Compiler added support for @abstract on classes and methods at the type system level in 2016. For new JavaScript code, the @abstract JsDoc should be used in favor of goog.abstractMethod.",const:"Marks a variable as read-only. The compiler can inline `@const` variables, which optimizes the JavaScript code.  \nThe type declaration is optional.  \nThe compiler produces a warning if a variable marked with `@const` is assigned a value more than once. If the variable is an object, note that the compiler does not prohibit changes to the properties of the object.  \n```js\n/** @const */ var MY_BEER = 'stout';  // NOTE: in most cases, you should instead use the const keyword here.\n\n/**\n * My namespace's favorite kind of beer.\n * @const {string}\n */\nmynamespace.MY_BEER = 'stout';\n\nclass User {\n  /** @param {string} beer */\n  constructor(beer) {\n    /**\n     * My user's favorite kind of beer.\n     * @const {string}\n     */\n    this.myBeer = beer;\n  }\n}\n```\nThe `@const` annotation is also how aliases for interfaces, records, classes and enums are annotated. Without that annotation the alias symbol will not be recognized in a type position.  \n```js\n/** @enum {number} */\nconst E = {...};\n\n/** @const */\nconst EAlias = E; \n```\nNote that repeating the original annotation, e.g. `@enum` at the alias declaration site is not allowed.  \n```js\n/** @enum */\nconst EAlias = E;  // wrong\n```\n",constructor:"Marks a function as a constructor.  The compiler requires a `@constructor` annotation for any function that is used with the `new` keyword.  @constructor should be omitted from EcmaScript `class` constructor methods and `goog.defineClass` constructor methods.  \n```js\n/**\n * A rectangle.\n * @constructor\n */\nfunction GM_Rect() {\n  ...\n}\n```\n",define:"Indicates a constant that can be overridden by the compiler at compile-time. With the example on the left, you can pass the flag `--define='ENABLE_DEBUG=false'` to the compiler to change the value of `ENABLE_DEBUG` to false. The type of a defined constant can be `number`, `string` or `boolean`. Defines are only allowed in the global scope.  \n```js\n/** @define {boolean} */\nvar ENABLE_DEBUG = true;\n\n/** @define {boolean} */\ngoog.userAgent.ASSUME_IE = false;\n```\n",deprecated:"Warns against using the marked function, method, or property should not be used. Using a deprecated method produces a compiler warning.  \n```js\n/**\n * Determines whether a node is a field.\n * @return {boolean} True if the contents of\n *     the element are editable, but the element\n *     itself is not.\n * @deprecated Use isField().\n */\nBN_EditUtil.isTopEditableField = function(node) {\n  ...\n};\n```\n",dict:"`@dict` is used to create objects with a variable number of properties. When a constructor (Foo in the example) is annotated with `@dict`, you can only use the bracket notation to access the properties of Foo objects. The annotation can also be used directly on object literals.  \n```js\n/**\n * @constructor\n * @dict\n */\nfunction Foo() {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\nor using the class keyword:  \n```js\n/** @dict */\nclass Foo {}\nvar obj1 = new Foo();\nobj1['x'] = 123;\nobj1.x = 234;  // warning\n\nvar obj2 = /** @dict */ { 'x': 321 };\nobj2.x = 123;  // warning\n```\n",enum:"Specifies an enum, which is a type with a specific finite number of possible values, often strings or numbers. `@enum` tag must be followed by a type expression.  If the type of an enum is omitted, number is assumed.  \nThe type label of an enum applies to each property of the enum. For example if an enum has type number, each of its enumerated properties must be a number.  \n```js\n/**\n * Enum for tri-state values.\n * @enum {number}\n */\nproject.TriState = {\n  TRUE: 1,\n  FALSE: -1,\n  MAYBE: 0\n};\n```\n",export:"When static or prototype properties are marked with `@export` and the compiler is run with the `--generate_exports` flag, a corresponding `goog.exportSymbol` statement will be generated:  \n```js\n/** @export */\nfoo.MyPublicClass.prototype.myPublicMethod = function() {\n  // ...\n};\n```\n```js\ngoog.exportSymbol('foo.MyPublicClass.prototype.myPublicMethod',\nfoo.MyPublicClass.prototype.myPublicMethod);\n```\nYou can write `/** @export {SomeType} */` as a shorthand for `/** @export @type {SomeType} */`.  \nWhen instance properties are marked with `@export`, the compiler backs off property renaming on that property in the entire compilation unit.  \n```\nclass C {\n  constructor() {\n    /** @export {number} */\n    this.foo = 0;\n    /** @type {number} */\n    this.bar = 0;\n  }\n}\n\n```\nbecomes after property renaming  \n```\nclass C {\n  constructor() {\n    this.foo = 0; //unrenamed\n    this.a = 0;   // renamed\n  }\n}\n\n```\nCode that uses the @export annotation must either:  \n\n<li>include `closure/base.js`, or</li>\n<li>define both `goog.exportSymbol` and `goog.exportProperty` with the same method signature in their own codebase.</li>\n  \n",extends:"Marks a class or interface as inheriting from another class. A class marked with `@extends` must also be marked with either `@constructor`, `@interface`, or `@record`.  \n\nNote: `@extends` does not cause a class to inherit from another class. The annotation simply tells the compiler that it can treat one class as a subclass of another during type-checking.\n  \nFor an example implementation of inheritance, see the Closure Library function `goog.inherits()`.  \n```js\n/**\n * Immutable empty node list.\n * @constructor\n * @extends {goog.ds.BasicNodeList}\n */\ngoog.ds.EmptyNodeList = function() {\n  ...\n};\n```\nWhile only one `@extends` is allowed with @constructor, it is allowed to use more than one for `@interface` or `@record`.  \n```js\n/**\n * @interface\n * @extends {SuperInterface1}\n * @extends {SuperInterface2}\n */\nfunction SomeInterface() {\n  ...\n};\n```\n\nNote: `@extends` is implicit with `class` expressions but can be used explicitly to, for example, provide type arguments to the super class type.\n  \n",externs:"Declares an externs file.  \nFiles annotated with @externs in the @fileoverview block are treated as externs files even if they appear compilation units regular srcs.  \nFor example:  \n```js\n/**\n * @fileoverview This is an externs file.\n * @externs\n */\n\nvar document;\n```\n",fileoverview:"Makes the comment block provide file level information including suppressions.  \nFor example:  \n```js\n/**\n * @fileoverview Utilities for doing things \n */\n```\n",final:"On a class, indicates that this class is not allowed to be extended. On a method, indicates that no subclass is allowed to override that method.  \n```js\n/**\n * An old-style class that cannot be extended.\n * @final\n * @constructor\n */\nsloth.MyFinalClass = function() { ... }\n\n/**\n * A class that cannot be extended.\n * @final\n */\nsloth.MyFinalClass = class { ... }\n\n/**\n * A method that cannot be overridden.\n * @final\n */\nsloth.MyFinalClass.prototype.method = function() { ... };\n```\n",implements:"Used with `@constructor` to indicate that a class implements an interface.  \nThe compiler produces a warning if you tag a constructor with `@implements` and then fail to implement all of the methods and properties defined by the interface.  \n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * @constructor\n * @implements {Shape}\n */\nfunction Square() {};\nSquare.prototype.draw = function() {\n  ...\n};\n```\n",implicitCast:"This annotation can only appear in externs property declarations. The property has a declared type, but you can assign any type to it without a warning. When accessing the property, you get back a value of the declared type. For example, `element.innerHTML` can be assigned any type, but will always return a string.  \n```js\n/**\n * @type {string}\n * @implicitCast\n */\nElement.prototype.innerHTML;\n```\n",inheritDoc:"Indicates that a method or property of a subclass intentionally hides a method or property of the superclass, and has exactly the same documentation. Note that the `@inheritDoc` tag implies the `@override` tag, `@override` is preferred.  \n```js\n/** @inheritDoc */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\nThis tag is not required when a class extends a superclass or implements an interface - in those cases the documentation is automatically inherited. This tag is mainly useful to catch typos such as a method named `foobar1` in the superclass and mistakenly named `foobar2` in the subclass.  \n",interface:"Marks a function as an interface. An interface specifies the required members of a type. Any class that implements an interface must implement all of the methods and properties defined on the interface's prototype. See `@implements`.  \nThe compiler verifies that interfaces are not instantiated. If the `new` keyword is used with an interface function, the compiler produces a warning.  \nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)  \n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",lends:'Indicates that the keys of an object literal should be treated as properties of some other object. This annotation should only appear on object literals.  \nNotice that the name in braces is not a type name like in other annotations. It\'s an object name. It names the object to which the properties are lent. For example, `@type {Foo}` means "an instance of Foo", but `@lends {Foo}` means "the constructor Foo".  \nThe JSDoc Toolkit docs have more information on this annotation.  \n```js\ngoog.object.extend(\n    Button.prototype,\n    /** @lends {Button.prototype} */ ({\n      isButton: function() { return true; }\n    }));\n```\n',license:"Tells the compiler to insert the associated comment before the compiled code for the marked file. This annotation allows important notices (such as legal licenses or copyright text) to survive compilation unchanged. Line breaks are preserved.  \n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",modifies:'`@nosideeffects` indicates that a call to the declared function has no side effects. This annotation allows the compiler to remove calls to the function if the return value is not used.  This is not a signal that the function is "pure": it may still read mutable global state.  \n`@modifies {this}` signals that only direct properties of the provided `this` are modified.  Calls to these functions may be removed if the "this" object is known to be otherwise unused.  \n`@modifies {arguments}` signals that only direct properties of the provided arguments are modified.  Calls to these functions may be removed if the parameters provided to the function are otherwise known to be unused.  \nThese annotations are only allowed in extern files.  \n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n',noalias:'Used in an externs file to indicate to the compiler that the variable or function should not be aliased as part of the "alias externals" pass of the compiler, is not enabled by default and only available through the Java API.  \nFor example:  \n```js\n/** @noalias */\nfunction Range() {}\n```\n',nocollapse:"Denotes a property that should not be collapsed by the compiler into a variable. The primary use for `@nocollapse` is to allow exporting of mutable properties. Note that non-collapsed properties can still be renamed by the compiler. If you annotate a property that is an object with `@nocollapse`, all its properties will also remain uncollapsed.  \n```js\n/**\n * A namespace.\n * @const\n */\nvar foo = {};\n\n/**\n * @nocollapse\n */\nfoo.bar = 42;\n\nwindow['foobar'] = foo.bar;\n```\n",nocompile:"For example:  \n```js\n/** @nocompile */\n```\nUsed at the top of a file to tell the compiler to parse this file but not compile it. Code that is not meant for compilation and should be omitted from compilation tests (such as bootstrap code) uses this annotation.  Most code should use other means such as a `@define` to change behavior.  \n",noinline:"(EXPERIMENTAL) Denotes a function or variable that should not be inlined by the optimizations.  \nCurrently, this annotation works in the common case, but there may be other code paths in the compiler that still cause inlining. See [https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914](https://github.com/google/closure-compiler/issues/2751#issuecomment-353484914) for more background and file a feature request if you have another use case for this annotation.  \n```js\n/** @noinline */\nfunction dontInlineMe() {\n  return 0;\n}\n/** @noinline */\nconst orMe = 1;\nconsole.log(dontInlineMe() + orMe);\n```\n",nosideeffects:'`@nosideeffects` indicates that a call to the declared function has no side effects. This annotation allows the compiler to remove calls to the function if the return value is not used.  This is not a signal that the function is "pure": it may still read mutable global state.  \n`@modifies {this}` signals that only direct properties of the provided `this` are modified.  Calls to these functions may be removed if the "this" object is known to be otherwise unused.  \n`@modifies {arguments}` signals that only direct properties of the provided arguments are modified.  Calls to these functions may be removed if the parameters provided to the function are otherwise known to be unused.  \nThese annotations are only allowed in extern files.  \n```js\n/** @nosideeffects */\nfunction noSideEffectsFn1() {\n  ...\n};\n\n/** @nosideeffects */\nvar noSideEffectsFn2 = function() {\n  ...\n};\n\n/** @nosideeffects */\na.prototype.noSideEffectsFn3 = function() {\n  ...\n};\n```\n',override:"Indicates that a method or property is part of an interface implemented by the class or it intentionally hides a method or property of the superclass. If no other annotations are included, the method or property automatically inherits annotations from its superclass.  \n```js\n/**\n * @return {string} Human-readable representation of\n *     project.SubClass.\n * @override\n */\nproject.SubClass.prototype.toString = function() {\n  ...\n};\n```\nAdding or omitting the `@override` tag does not change what type the compiler infers for a method in a class that extends a superclass or implements an interface. The method's documentation is always automatically inherited from the overridden method in those cases. However, the recommended compiler flagset includes warnings for missing and spurious `@override` annotations. These warnings are mainly useful to catch typos, like a method named `foobar1` in the superclass and mistakenly named `foobar2` in the subclass.  \n",package:"Marks a member or property as package private. Only code in the same directory can access names marked @package. In particular, code in parent and child directories cannot access names marked `@package`.  \nPublic constructors can have `@package` properties to restrict the methods that callers outside the directory can use. On the other hand, `@package` constructors can have public properties to prevent callers outside the directory from directly instantiating a type.  \n```js\n/**\n * Returns the window object the foreign document resides in.\n *\n * @return {Object} The window object of the peer.\n * @package\n */\ngoog.net.xpc.CrossPageChannel.prototype.getPeerWindowObject = function() {\n  // ...\n};\n```\n",param:"Used with method, function and constructor definitions to specify the types of function arguments.  \nThe `@param` tag must be followed by a type expression.  \nAlternatively, you can annotate the types of the parameters inline (see function foo in the example).  \n```js\n/**\n * Queries a Baz for items.\n * @param {number} groupNum Subgroup id to query.\n * @param {string|number|null} term An itemName,\n *     or itemId, or null to search everything.\n */\ngoog.Baz.prototype.query = function(groupNum, term) {\n  ...\n};\n\nfunction foo(/** number */ a, /** number */ b) {\n  return a - b + 1;\n}\n```\n",polymer:"Indicates that a `class` has Polymer semantics. Only affects the compiler when the `--polymer_pass=2` flag is specified. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize Polymer classes and add appropriate type information.  \n```js\n/**\n * A Polymer class\n * @polymer\n */\nclass MyElement extends Polymer.Element {}\n```\n",polymerBehavior:"Indicates that a global object is usable as a Polymer behavior. Allows the [Polymer Pass](https://github.com/google/closure-compiler/wiki/Polymer-Pass) to recognize behaviors and copy type information over to the using Polymer definition. Behavior objects must be global.  \n```js\n/** @polymerBehavior */\nvar FunBehavior = {\n  /** @param {string} funAmount */\n  doSomethingFun: function(funAmount) { alert('Something fun!'); },\n};\n\nvar A = Polymer({\n  is: 'x-custom',\n  behaviors: [ FunBehavior ],\n});\n```\n",preserve:"Tells the compiler to insert the associated comment before the compiled code for the marked file. This annotation allows important notices (such as legal licenses or copyright text) to survive compilation unchanged. Line breaks are preserved.  \n```js\n/**\n * @preserve Copyright 2009 SomeThirdParty.\n * Here is the full license text and copyright\n * notice for this file. Note that the notice can span several\n * lines and is only terminated by the closing star and slash:\n */\n```\n",private:"Marks a member as private. Only code in the same file can access global variables and functions marked `@private`. Constructors marked `@private` can only be instantiated by code in the same file and by their static and instance members.  \nThe public static properties of constructors marked `@private` may also be accessed anywhere.  \n```js\n/**\n * Handlers that are listening to this logger.\n * @private {Array<Function>}\n */\nthis.handlers_ = [];\n```\n",protected:"Indicates that a member or property is protected. A property marked `@protected` is accessible to:  \n\n<li>all code in the same file</li>\n<li>static methods and instance methods of any subclass of the class on which the property is defined.</li>\n  \n```js\n/**\n * Sets the component's root element to the given element.\n * Considered protected and final.\n * @param {Element} element Root element for the component.\n * @protected\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",public:"Indicates that a member or property is public. A property marked `@public` is accessible to all code in the any file.  This is the implicit default and rarely used.  This is not used to indicate that name should be preserved in obfuscating builds see `@export`.  \n```js\n/**\n * @public\n */\ngoog.ui.Component.prototype.setElementInternal = function(element) {\n  // ...\n};\n```\n",record:"Marks a function as an interface. An interface specifies the required members of a type. Any class that implements an interface must implement all of the methods and properties defined on the interface's prototype. See `@implements`.  \nThe compiler verifies that interfaces are not instantiated. If the `new` keyword is used with an interface function, the compiler produces a warning.  \nFor the differences between @record and @interface, see [Structural Interfaces](https://github.com/google/closure-compiler/wiki/Structural-Interfaces-in-Closure-Compiler)  \n```js\n/**\n * A shape.\n * @interface\n */\nfunction Shape() {};\nShape.prototype.draw = function() {};\n\n/**\n * A polygon.\n * @interface\n * @extends {Shape}\n */\nfunction Polygon() {};\n\nPolygon.prototype.getSides = function() {};\n```\n",return:"Specifies the return types of method and function definitions. The `@return` tag must be followed by a type expression.  \nAlternatively, you can annotate the return type inline (see function `foo` in the example).  \nIf a function that is not in externs has no return value, you can omit the `@return` tag, and the compiler will assume that the function returns `undefined`.  \n```js\n/**\n * Returns the ID of the last item.\n * @return {string} The hex ID.\n */\ngoog.Baz.prototype.getLastId = function() {\n  ...\n  return id;\n};\n\nfunction /** number */ foo(x) { return x - 1; }\n```\n",struct:"`@struct` is used to create objects with a fixed number of properties. When a constructor (Foo in the example) is annotated with `@struct`, you can only use the dot notation to access the properties of Foo objects, not the bracket notation. Also, you cannot add a property to a Foo instance after it's constructed. The annotation can also be used directly on object literals.  \nClasses created with the ES class keyword or with `goog.defineClass` are `@struct` by default, so this annotation is only needed for old-style `@constructor` functions.  \n```js\n/**\n * @constructor\n * @struct\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = \"qwerty\";  // OK\nobj1['x'] = \"asdf\";  // warning\nobj1.y = 5;  // warning\n\nvar obj2 = /** @struct */ { x: 321 };\nobj2['x'] = 123;  // warning\n```\n",suppress:"Suppresses warnings. Warning categories are separated by | or ,.\nFor a list of warning names, take a look at [Warnings](https://github.com/google/closure-compiler/wiki/Warnings).  \nFor example:  \n```js\n/**\n * @suppress {deprecated}\n */\nfunction f() {\n  deprecatedVersionOfF();\n}\n```\n",template:"See [Generic Types](https://github.com/google/closure-compiler/wiki/Generic-Types).  \n```js\n/**\n * @param {T} t\n * @constructor\n * @template T\n */\nContainer = function(t) { ... };\n```\n",this:"Specifies the type of the object to which the keyword this refers within a function. The `@this` tag must be followed by a type expression.  \nTo prevent compiler warnings, you must use a `@this` annotation whenever `this` appears in a function that is neither a prototype method nor a function marked as a `@constructor`.  \n```js\n    /**\n     * Returns the roster widget element.\n     * @this {Widget}\n     * @return {Element}\n     */\n    function() {\n      return this.getComponent().getElement();\n    };\n```\n",throws:"Used to document the exceptions thrown by a function. The type checker does not currently use this information. It is only used to figure out if a function declared in an externs file has side effects.  \n```js\n/**\n * @throws {DOMException}\n */\nDOMApplicationCache.prototype.swapCache = function() { ... };\n```\n",type:"Identifies the type of a variable, property, or expression. The `@type` tag must be followed by a type expression. You can also write the type annotation inline and omit `@type`, as in the second example.  \n```js\n/**\n * The message hex ID.\n * @type {string}\n */\nvar hexId = hexId;\nvar /** string */ name = 'Jamie';\n```\n",typedef:"Complex types (including unions, and record types) can be aliased for convenience and maintainability using a typedef. These annotations can be long, but can be split over multiple lines for readability.  \n```js\n/** \n * @typedef {{\n *            foo:string,\n *            bar:number,\n *            foobar: (number|string)\n *          }}\n */\nvar Mytype;\n\n/** @param {MyType} bar */\nfunction foo(bar) {}\n```\n",unrestricted:'Indicates that a class is neither a `@struct` type nor a `@dict` type. This is the default for old Closure-style classes created with a `@constructor` function. Both `goog.defineClass` and the ES class keyword produce classes which are `@struct` by default, so will need to be explicitly labeled `@unrestricted`.  \n```js\n/**\n * @constructor\n * @unrestricted (This annotation is optional)\n */\nfunction Foo(x) {\n  this.x = x;\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\nor using the class keyword:  \n```js\n/** @unrestricted */\nclass Foo {\n  constructor(x) {\n    this.x = x;\n  }\n}\n\nvar obj1 = new Foo(123);\nvar someVar = obj1.x;  // OK\nobj1.x = "qwerty";  // OK\nobj1[\'x\'] = "asdf";  // OK\nobj1.y = 5;  // OK\n```\n'},inlineDocs:{}}}}},e={},t=function t(o){var r=e[o];if(void 0!==r)return r.exports;var i=e[o]={exports:{}};return n[o](i,i.exports,t),i.exports}(3);module.exports=t})();